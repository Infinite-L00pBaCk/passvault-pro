# passvault-pro
PassVault Pro is a fully offline password manager that runs entirely in your browser. Create local accounts secured with a username + password or 4-digit PIN. Generate strong passwords, store them in a per-user encrypted vault, audit your security health, and export your data anytime — no internet, no backend, no accounts required.
